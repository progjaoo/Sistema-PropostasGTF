{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/db run generate && pnpm --filter @workspace/api-server run build && PORT=21709 BASE_PATH=/ pnpm --filter @workspace/proposta run build",
  "outputDirectory": "artifacts/proposta/dist/public",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
}
